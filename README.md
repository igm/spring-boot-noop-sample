# Spring Boot noop-buildpack Sample
Simple Spring Boot Application that can be pushed to CloudFoundry using "noop-buildpack"

```
$ mvn package
$ cf push sample-app -b https://github.com/igm/noop-buildpack
```


